{
  "name": "dive-browser",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter @dive/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "check": "cargo fmt --all -- --check && pnpm typecheck && pnpm lint && pnpm test && pnpm --filter @dive/desktop exec vite build && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace",
    "release:next": "node scripts/release/resolve-release.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}

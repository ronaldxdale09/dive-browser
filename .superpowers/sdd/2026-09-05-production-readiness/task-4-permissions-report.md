# Task 4 permission safety slice — implementation checkpoint

Status: supported permission-cache slice implemented and locally native-qualified; broader legacy migration remains OPEN. Full temporary native-prompt support and strict iframe-origin isolation remain OPEN. No OS permission or media device request was triggered by this subagent.

Implemented original native callback bridge, opaque IDs, explicit media/prompt capability distinction, authoritative profile + actual CEF container + canonical HTTP(S) origin keys, scoped Settings and remembered persistence, per-document media decisions, native deadline/cancel/navigation/closure cleanup, exact browser generation attachment, and unknown-bit/origin default refusal. Removed the unscoped CDP Browser.setPermission override and unused renderer media guard. Chrome clipboard is narrowly scoped to trusted native chrome label/window and bundled native top URL. Root owns broader native IPC security.

Native prompt ACCEPT/DENY was proven persistent from CEF2384915 and Chromium151.0.7922.174 source. Page-only native prompt choices now fail at the backend and are omitted in UI. Timeout/default/cancel use DISMISS. UI-thread answers complete inline, avoiding a queued allow overtaking a later Settings Ask. Direct device media callbacks do not persist Chromium content settings, so document lifetime remains supported there.

Native reconciliation now executes before browser creation in each true RequestContext. It clears only supported scalar/structured permission defaults and exception dictionaries, verifies readback, and fails closed on missing preference/write/readback failure. Weak leases compare actual shared contexts and do not retain closed contexts globally. Supported legacy embargo keys are removed while unrelated permission kinds and metadata remain. Settings uses weak native-context handles registered by authoritative scope independently of live tabs. Handles deduplicate by native identity and expired handles are pruned, so a retained30s native timeout cannot hide an old grant after the last tab closes. Exact origin/type reset covers structured geolocation and corresponding embargo; matching pending/temporary requests are revoked before the new DIVE setting is written. Legacy DIVE entries are preserved but ignored; Settings explains reapproval.

Source-backed limitations: a no-frame CEF prompt is refused when requesting origin differs from top origin or browser contains multiple frames. However Chromium may delegate a remembered top-origin grant to a subsequently inserted child without another callback. Strict iframe-origin isolation remains OPEN. CEF prompt has no AcceptThisTime and notification display rechecks native permission, so resetting immediately or on a guessed delay cannot honestly implement temporary notifications/geolocation. Media frame checks reject mismatched fallback frames but do not claim complete OOPIF provenance. A current media stream is not forcibly stopped by changing a remembered future-request decision; native stream revocation still needs explicit qualification.

Regression evidence:
- Policy red failed legacy/global scope and canonical separation; green3: /tmp/dive-permission-policy-{red,green}.log.
- Opaque request admission, lifetime and pending cancellation red/green: /tmp/dive-permission-requests-{red,green}.log.
- Grouped remembered decisions rollback on second DB write failure: /tmp/dive-permission-atomic-{red,green}.log.
- Reviewer stale attach and unsupported native-page duration: exactly2 red failures and7 green request tests, /tmp/dive-permission-review-{red,green}.log.
- Closed-last-tab context regression red1 then green15: /tmp/dive-permission-closed-context-{red,green}.log; verifies reset still reaches surviving native context, sibling scope is untouched, and expired handles are removed.
- Settings revoke red demonstrates stale page decision survives; /tmp/dive-permission-revoke-red.log. Final desktop permission suite14 pass: /tmp/dive-permission-desktop-final.log.
- Native cache reset red2, then green2: /tmp/dive-permission-cache-{red,green}.log.
- Native origin mismatch red1: /tmp/dive-permission-origin-red.log; final native9 pass: /tmp/dive-permission-native-reconcile-green.log.
- UI page-only native prompt red1: /tmp/dive-permission-duration-ui-red.log. Current full frontend639 pass (89 files): /tmp/dive-permission-ui-reconcile.log.
- Typecheck pass: /tmp/dive-permission-typecheck-reconcile.log. ESLint passes: /tmp/dive-permission-eslint-reconcile.log. Owned Rust sources formatted. Root will run integrated strict Clippy.

No completion or release claim. Parent handles native disposable-profile qualification with mock keychain and the remaining audit roadmap.

## Latest native boundary and review corrections

Native diagnostic fingerprint `a15208bf296612cd18d8498774af17b5f387ee5bf70f1f9c696ed3e471cdf98b` failed before chrome creation, status-6: `Cannot reset native preference profile.content_settings.exceptions.media_stream_camera` with empty error. Evidence: `target/network-probe-1788558209978626000/native.log`. Root cause is pinned cef151.8.1 `CefString::default()` converting to a NULL struct pointer, invalid for SetPreference's required mutable error out-parameter. Native setters now use an allocated empty `CefString::from("")`, and embargo APIs receive a non-null empty embedding string. No preference names or context-init timing were weakened, and reset/readback remain mandatory. Native rerun pending.

Review also identified that CEF DISMISS can synchronously recreate Chromium embargo after a Settings reset. New `revoke_and_reset` cancels original pending callbacks first, then performs final native reset/readback. Dismissed UI IDs are emitted even if native reset or subsequent SQLite write fails. Controlled callback-mutates-embargo regression: red1 `/tmp/dive-permission-reset-order-red.log`; green16 `/tmp/dive-permission-reset-order-green.log`.

New `permission_probe.rs` is a device-free native qualification helper, enabled only by parent opt-in lifecycle hook. It seeds native notification and structured geolocation settings directly, validates shared-context reuse and distinct-container separation, waits for actual native close and Registry-session removal, then tests real Settings Ask followed by reopen while retaining the original context lifetime. It checks an unrelated sibling origin is preserved. It creates only disposable-profile fixtures; no device or OS permission request. Strict compile/gate passes; execution remains parent's native task.

2026-09-05 Final combined native checkpoint: exact bundle SHA256 `4f0330bfc6932aba7dbb5e9e2ba81463c862b2e065639759a4e82238071d4534` passed both ten-response capture matrices (small-cache2.54s, large-cache2.32s), including plain/gzip/Blob/service-worker oversized omission before body-fetch dispatch and two browser cache rows backed by one HTTP request. Evidence `target/network-probe-1788560265670862000` and `target/network-probe-1788560281999130000`. Device-free native permission cache readback, shared-container reuse, separate-container isolation, closed-last-tab Settings Ask/reset/reopen, and sibling-origin preservation passed in both runs and four additional same-profile lifecycle cycles. Lifecycle runs2.28/1.78/1.94/1.79s passed history, IPC, popout close/reattach, natural exit/helper drain; incomplete startup returned1 (`target/lifecycle-probes-1788560302033011000`). All disposable launches explicitly use mock keychain and volatile AppKit ignore-state arguments; no personal keychain/defaults or installed app changed.

Readiness diagnostics exposed native navigation transitions in several read-only CDP polls. A shared bounded helper retries only the two identified -32000 navigation errors on an open session; closed sessions, unrelated errors and JS exceptions fail. History/IPC mutations are never replayed. Controlled transport regressions and source review passed. Final full Rust workspace424 tests pass (`/tmp/dive-capture-permission-rust-final5.log`), native permission9 pass, Python harness13 pass, frontend639/typecheck/ESLint/Vite from latest integrated gate pass, strict Clippy and diff whitespace checks pass. This qualifies the bounded capture and supported permission-cache slice locally, not the entire browser. Broader legacy native grants, temporary prompt/iframe semantics, RSS reclaim, remaining feature workflows and signed release qualification remain OPEN.


## Legacy scalar migration follow-on (after checkpoint 5057adb)

Status: source implementation and focused regressions complete; native qualification of this expansion is pending. No binary build, native launch, or commit performed by this subagent. The earlier exact-bundle evidence above covers the previous six-setting implementation only.

Startup cleanup now includes 17 additional scalar permission stores: `ar`, `vr`, `hand_tracking`, `midi_sysex`, `camera_pan_tilt_zoom`, `captured_surface_control`, `idle_detection`, `local_fonts`, `keyboard_lock`, `pointer_lock`, `window_placement`, `local_network`, `loopback_network`, `web_app_installation`, `storage_access`, `top_level_storage_access`, and `automatic_downloads`. All are registered on the desktop platforms with native ASK defaults. Each regular and partitioned exception dictionary is reset and read back; the default is reset and must read back ASK. Clearing whole permission-type dictionaries at startup covers requesting/embedding pairs without guessing `(origin, origin)` semantics. These names are deliberately separate from the six supported origin-specific Settings types.

Sensors is an eighteenth additional store, but Chromium defaults it to ALLOW. Startup clears its grant dictionaries and explicitly writes/readbacks ASK, which is a registered valid value. Windows-only `protected_media_identifier` is the nineteenth additional store: it is absent on macOS/Linux and accepts only ALLOW/BLOCK on Windows, so startup explicitly writes/readbacks BLOCK there. A missing expected preference, rejected write, or ineffective readback fails initialization. This does not change unrelated default-ALLOW browser settings, such as cookies, JavaScript, sound, or federated identity API.

Evidence: `/tmp/dive-permission-legacy-red.log` shows two behavioral failures: existing AR grants survive and the sensor default remains ALLOW. `/tmp/dive-permission-legacy-green.log` passes all 11 native permission unit tests after implementation. Fixtures seed both normal and partitioned requesting/embedding grants plus old global ALLOW defaults independently of the production list, preserve unrelated cookies and unresolved chooser/protocol/identity records, and reject a native reset that returns success without clearing a partitioned storage-access grant. Windows execution has not been qualified on this macOS host.

Pinned primary mappings:
- [CEF permission prompt mapping at 2384915](https://github.com/chromiumembedded/cef/blob/2384915/libcef/browser/permission_prompt.cc) maps exposed masks to Chromium request types.
- [Chromium 151.0.7922.174 request_type.cc](https://github.com/chromium/chromium/blob/151.0.7922.174/components/permissions/request_type.cc) maps those request types to content settings, including storage-access scopes and platform-conditional protected media. The [content settings registry](https://github.com/chromium/chromium/blob/151.0.7922.174/components/content_settings/core/browser/content_settings_registry.cc) gives actual preference names, valid values, defaults, and platforms. Window management deliberately retains the legacy `window-placement` name; preference serialization uses underscores.
- [DownloadRequestLimiter](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/download/download_request_limiter.cc) proves `MultipleDownloads` Accept writes AUTOMATIC_DOWNLOADS even though the generic reverse request-type mapping returns nullopt.

Unresolved legacy stores remain OPEN; the implementation does not silently erase unrelated data or claim these grants are removed:
- File-system access uses FILE_SYSTEM_ACCESS_CHOOSER_DATA and persisted per-path read/write objects through [ChromeFileSystemAccessPermissionContext](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/file_system_access/chrome_file_system_access_permission_context.cc). Clearing read/write guard scalars alone cannot clear these objects. Correct cache invalidation and startup service timing still need qualification; site files themselves must remain intact.
- Protocol-handler Accept calls [ProtocolHandlerRegistry](https://github.com/chromium/chromium/blob/151.0.7922.174/components/custom_handlers/protocol_handler_registry.cc), registers a user handler, sets its default, and saves user preference lists. It loads policy/user registrations into an in-memory registry. A safe migration needs the real registry lifecycle/API and must preserve managed/predefined handlers; no fabricated content-setting key was used.
- Identity provider registration persists object grants in FEDERATED_IDENTITY_IDENTITY_PROVIDER_REGISTRATION (`fedcm_idp_registration`), shown by [FederatedIdentityIdentityProviderRegistrationContext](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/webid/federated_identity_identity_provider_registration_context.cc). It is distinct from `webid_api` and ordinary identity cookies/sign-in/sharing data. Full prompt-callback linkage and object-cache invalidation timing remain unqualified.
- Disk quota has no scalar reverse mapping in the pinned request-type source. Its current reachable persistence path has not been proven. No site quota database, durable-storage grant, or site files were guessed as its replacement.

Unsupported permission embargo records remain intact because this slice never offers grants for those kinds. Their deny/ignore counts cannot revive an ALLOW; the existing supported-kind embargo cleanup remains unchanged. New unknown-mask prompts still fail closed. This migration does not resolve temporary native-prompt support, cached iframe delegation, or active media-stream revocation; these remain full-goal work.

Follow-on strict validation: `cargo clippy --workspace --all-targets -- -D warnings` passed (`/tmp/dive-permission-legacy-clippy.log`); `git diff --check` passed. Direct rustfmt used the vendored 2-space/edition2024 convention. Source review requested from the existing reviewer; no new agent created.


Legacy migration native probe prepared: the existing opt-in permission probe now seeds real AR permission ALLOW, a requesting/embedding pair in the native partitioned storage-access preference, and the native sensor default ALLOW. All seeds must read back before it calls the production `initialize()` implementation through an explicit diagnostic bridge. It then requires AR ASK, the partitioned dictionary empty, and both sensor preference/effective permission ASK. `DIVE_PERMISSION_LEGACY_PROBE` is printed only after those checks. The original six-setting/container/closed-context proof runs afterward unchanged. This exercises real native preference storage and production migration; it does not claim that a cross-site page actually exercised storage-access delegation. The new method requires CEF UI and `DIVE_PERMISSION_CACHE_PROBE=1`, never grants a capability, and has no renderer IPC command.

Probe oracle regression red1 `/tmp/dive-permission-legacy-probe-red.log`, then both probe tests green `/tmp/dive-permission-legacy-probe-green.log`. A missing `ImplPreferenceManager` import was caught at compile and corrected. Root owns the subsequent integrated strict gate and native launch; no native result is claimed here. Existing reviewer independently confirmed the scalar mapping/default/platform treatment with no actionable issue. All owned sources are formatted and diff whitespace checks pass; source edits stopped for root's integrated build.

2026-09-05 Avatar/legacy-permission checkpoint: worker-based generation, exact saved artwork compatibility, atomic bounded cache, and fixed pre-generated privacy icon implemented/reviewed. Main App552292→163390 minified bytes; zero DiceBear modules remain in App. Exact native95f8693172336f4c33417cfca9b23c9fb070d39b782799104a47913bc0ee164d verified cold artwork after FCP and three warm restarts without worker creation, plus expanded legacy AR/partitioned storage-access/sensor native reset and existing permission/history/IPC/lifecycle checks. Four normal runs/helper drains plus exit1 negative: target/lifecycle-probes-1788561111800548000. Startup8samples+warmed launch: cold median698.90ms/warm710.89ms/controls p95742.83ms, target/startup-probes-1788561146168712000. Full frontend650, Rust425, Python13, strict Clippy/typecheck/ESLint pass. Expanded mapping11 vendor permission tests pass; chooser/protocol/FedCM/disk-quota legacy stores and Windows-specific validation remain open. Full Task7 and browser goal remain open. Detailed report task-7-avatar-report.md.


## Chooser/protocol migration investigation and effective-service probe

Production migration remains OPEN; no post-initialization preference-only cleanup or pre-create disk mutation was implemented. Source review established two concrete hazards:
- [ProtocolHandlerRegistryFactory](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/custom_handlers/protocol_handler_registry_factory.cc) creates the registry with the profile. [ProtocolHandlerRegistry](https://github.com/chromium/chromium/blob/151.0.7922.174/components/custom_handlers/protocol_handler_registry.cc) copies prefs into live maps, saves those maps back, and uses a separate predefined-handler set when selecting user-defined handlers. Clearing prefs after load cannot revoke the live handler; serialized user entries do not reliably identify predefined status.
- [StoragePartitionImpl](https://github.com/chromium/chromium/blob/151.0.7922.174/content/browser/storage_partition_impl.cc) obtains the file-system permission context while initializing its manager. [ChromeFileSystemAccessPermissionContext](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/file_system_access/chrome_file_system_access_permission_context.cc) may load persisted objects in its constructor; [ObjectPermissionContextBase](https://github.com/chromium/chromium/blob/151.0.7922.174/components/permissions/object_permission_context_base.cc) retains an initialized object map that external preference writes do not invalidate.

The proposed pre-RequestContext JSON migration was rejected after tracing [ProfileManager](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/profiles/profile_manager.cc) and [StartupBrowserCreator](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/ui/startup/startup_browser_creator.cc): GetLastOpenedProfiles eagerly loads saved profile paths; fallback startup can invoke it before the runtime observes its first explicit per-container request. CreateProfileAsync reuses existing profiles. A process-local first-use set and global-context path comparison cannot detect all such live profiles. An atomic JSON rename still would not make that write safe. A robust native migration needs actual service revocation or a native loaded-profile exclusion/reservation hook, neither exposed directly by the current CEF RequestContext ABI.

Opt-in diagnostic prepared instead: `DIVE_PERMISSION_WEBUI_PROBE=1` under the existing `DIVE_PERMISSION_CACHE_PROBE=1` path creates a brand-new generated container directory using exclusive create semantics, seeds its initial Preferences before any view opens, and refuses preexisting directories/files. This exception is diagnostic-only and proves a newly created fixture is not a live profile; it does not authorize rewriting existing profiles. The fixture contains two chooser origins with read-only grants to a file created by the probe, plus ordinary/app/extension/policy protocol handlers. All protocol defaults are false to avoid OS default registration. The native RequestContext cache path must equal the fixture path.

The probe opens `chrome://settings/handlers` and imports pinned [cr.ts / cr.js](https://github.com/chromium/chromium/blob/151.0.7922.174/ui/webui/resources/js/cr.ts) transports. [ProtocolHandlersHandler](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/ui/webui/settings/protocol_handlers_handler.cc) supplies `observeProtocolHandlers` / `setProtocolHandlers` and `removeHandler`. [SiteSettingsHandler](https://github.com/chromium/chromium/blob/151.0.7922.174/chrome/browser/ui/webui/settings/site_settings_handler.cc) supplies `getFileSystemGrants` / `revokeFileSystemGrants`, invoking real permission-context revocation. The probe must first observe all seeded effective objects, including the app identity. It then removes only the exact ordinary handler and one chooser origin, once each, and compares every remaining effective service entry. Native baseline handlers beyond fixtures are preserved and counted; a zero baseline must not be described as a positive predefined-handler test. It verifies the underlying file contents remain intact.

Each query has a deadline and listener cleanup; native evaluation and view close have outer deadlines. Mutations are never replayed after timeout/navigation errors. The captured native browser is closed on successful/failed evaluation, with close receipt verified, and the existing six-setting/legacy-scalar permission probes remain intact. FedCM/disk quota mappings and production initialization refactoring are untouched.

Evidence: actual included JS red3 `/tmp/dive-permission-webui-red.log` then green3; additional app-identity positive-fixture red1 `/tmp/dive-permission-webui-identity-red.log` then final green4 `/tmp/dive-permission-webui-green.log`. Rust permission-probe3 pass `/tmp/dive-permission-webui-rust.log`; typecheck and targeted ESLint pass `/tmp/dive-permission-webui-{typecheck,eslint}.log`; strict workspace Clippy passes `/tmp/dive-permission-webui-clippy.log`. The initial compile caught an unavailable direct uuid import in the fixture test; it now uses the existing core-generated container ID. No binary build, native launch, or commit was performed by this subagent. Shipping-CEF WebUI availability and effective-service behavior await parent native qualification; no successful result is claimed yet.

WebUI diagnostic native failure and routing correction: exact `2f7c87dc1ade00293280f38ab4b9942a52ebe14887b63d23020c446fe18bb5f2` passed welcome/avatar/history/IPC, then failed the 10-second WebUI deadline and naturally exited 1 (`target/lifecycle-probes-1788562673451595000/1-quit.log`). Local source established the probe passed `chrome://settings/handlers` through ordinary `commands::open_tab` address normalization, which excludes the `chrome` scheme and turns this no-dot input into a search query. This failure does not establish that shipping CEF disables Chrome Settings. The nearby ad-hoc signature warning is not attributed as the cause.

The probe now opens its inert data fixture through ordinary commands, waits for that initial navigation to commit, validates the captured native RequestContext path, and invokes `main_frame.load_url` once with the fixed Settings constant on CEF UI. This avoids both address normalization and a pending initial navigation overriding the Settings navigation. No user-provided internal URL or new IPC route is introduced. Readiness retains bounded renderer URL/readyState/title/chrome.send evidence; native read-only browser ID/validity/loading/main-frame URL snapshots are bounded to two seconds. Errors distinguish fixture/open/bootstrap/context-navigation/document/service/close phases. Service mutations are still sent once only, never replayed.

Diagnostic regressions: redirected/error-page and JavaScript-error readiness red1 `/tmp/dive-permission-webui-evidence-red.log`, then green; inert bootstrap without WebUI transport red1 `/tmp/dive-permission-webui-bootstrap-red.log`, then all three WebUI Rust tests pass `/tmp/dive-permission-webui-bootstrap-green.log`. Strict workspace Clippy passes `/tmp/dive-permission-webui-phase-clippy.log`. Source review requested on this correction. No native build, launch, or commit performed by this subagent; the corrected routing and effective native service behavior remain pending parent's exact-binary run.

Second WebUI native qualification fails for a different, source-confirmed reason. Exact bundle `c3e1fdab6491b1fde7802a4921f0d34984a5d2e96cf99bcc67e5bdaebc555744` (`target/lifecycle-probes-1788563487361168000/1-quit.log`) commits the correct inert data bootstrap, but fixed native Settings navigation starts then stops without changing the document. After the bounded 10-second wait, both native and renderer URLs still identify the data fixture; browser remains valid, loading is zero, chrome.send is undefined. Cleanup/natural exit 1 completes. This is not a successful effective-service proof.

Pinned source identifies the veto: [CEF AlloyBrowserHostImpl at 2384915](https://github.com/chromiumembedded/cef/blob/2384915/libcef/browser/alloy/alloy_browser_host_impl.cc) defines the allowed WebUI hosts at lines 66–92, omitting `settings`, and `MaybeAllowNavigation` lines 563–570 rejects every non-allowlisted chrome host. [CEF macOS WindowInfo](https://github.com/chromiumembedded/cef/blob/2384915/include/internal/cef_types_mac.h) lines 151–155 states that a parent view always selects Alloy style. The vendored runtime uses `WindowInfo::set_as_child` and sets that parent; changing its runtime_style alone cannot enable Chrome Settings on macOS. The DIVE page builder installs no navigation veto; the runtime request callback returns allow when no handler exists. DIVE's separate chrome navigation guards remain correct and unchanged.

No production or runtime allowlist workaround was applied. A feasible next opt-in diagnostic would need a parentless windowed Chrome-style CEF browser with the original captured RequestContext, an independent deny-by-default/lifecycle client and bounded DevTools transport, and real close receipt. It may create a visible standalone Chrome UI window; offscreen rendering also forces Alloy. Such a diagnostic needs separate integration and native qualification before any async production initialization design. The alternative is a custom CEF embedding API that invokes the actual chooser/protocol services. Effective chooser/protocol revocation and preservation remain OPEN. This source-only diagnosis changes no executable source and performs no native launch/build.

## Hidden Chrome Views diagnostic follow-on

Parent authorized a supported opt-in diagnostic host, not production migration. `WindowInfo.hidden=1` alone is insufficient: pinned [ChromeBrowserHostImpl::CreateBrowser](https://github.com/chromiumembedded/cef/blob/2384915/libcef/browser/chrome/chrome_browser_host_impl.cc) shows a parentless direct Chrome browser when no BrowserView is supplied. The actual supported hidden path is demonstrated by pinned [cefsimple SimpleWindowDelegate](https://github.com/chromiumembedded/cef/blob/2384915/tests/cefsimple/simple_app.cc): on macOS select `CEF_SHOW_STATE_HIDDEN`, create a Chrome BrowserView in a Chrome CefWindow, add the view and never call Show. The BrowserView branch of ChromeBrowserHostImpl skips implicit Show. The new `permission_hidden_view.rs` follows that pattern with no OSR or external parent view.

The existing inert Tauri fixture view remains solely to capture the actual initialized RequestContext. A separate native BrowserView uses that exact context and an independent diagnostic client. Before Settings navigation, before **every** outgoing CDP call, and after service success, it requires browser/BrowserView/window runtime CHROME, RequestContext sharing plus exact native cache path, actual window invisibility, no activation, and intact lifetime. [CefWindowImpl::IsVisible](https://github.com/chromiumembedded/cef/blob/2384915/libcef/browser/views/window_impl.cc) queries the actual native widget. The native host is macOS-only until other platforms are explicitly qualified.

The client cancels media, dismisses native prompts, rejects popups/downloads/file dialogs/focus, vetoes navigation outside the inert/Settings allowlist, and sets external OS protocol execution false. Existing fixture default flags remain false; service mutation still consists only of the single ordinary handler removal and single chooser-origin revocation after mandatory positive service baselines. Protected app/extension/policy/sibling entries and marker file must remain. No OS registration API or device request is invoked.

The native observer and UI-thread transport reuse `dive-cdp::CdpSession` rather than creating another request multiplexer. Incoming/outgoing messages are capped at 1MiB; malformed/oversized/wrong-browser replies, agent detach and native send failure close that session. Exact read-only readiness retry rules remain intact; service mutations are never retried. Creation, evaluation and cleanup are bounded. Normal success requires both `OnBeforeClose` for the captured browser and `OnWindowDestroyed`, in either order. Native handles and observer registration are released by those callbacks; the parent watchdog remains responsible for ultimate process cleanup on diagnostic failure.

Red2 invariant/dual-receipt tests: `/tmp/dive-permission-hidden-red.log`; all six WebUI/hidden Rust tests now pass `/tmp/dive-permission-hidden-green.log`, including actual diagnostic external-protocol callback refusal and download/navigation guards. Strict workspace Clippy passes `/tmp/dive-permission-hidden-clippy.log`. The pinned cef wrap macros themselves expand a reference-to-reference transmute; a documented local Clippy allowance covers that generated code, without changing the native ABI. Handwritten code adds no unsafe transmute. Source review is pending; no build/launch/commit performed by this subagent. Native invisibility, context sharing, Settings service availability and complete destruction remain unqualified until the parent's exact-binary run.

Hidden-host review correction: a BrowserView that succeeded before a failed Window creation could remain in an ownership cycle while waiting for callbacks from objects that never existed. Cleanup now explicitly takes/drops the unparented BrowserView off-lock and tracks each native object as Absent/Live/Closed. Failed partial creation needs a receipt for each object that actually became Live; an established successful probe still requires both browser and window Closed. An atomic closing flag is set before scheduling cleanup, checked before queued creation and before window creation, and late browser-created callbacks force-close while cancellation is active. Partial-creation regression red1 `/tmp/dive-permission-hidden-partial-red.log`, then all seven focused tests pass `/tmp/dive-permission-hidden-partial-green.log`. Final strict Clippy passes `/tmp/dive-permission-hidden-clippy-final.log`. Window preferred size follows cefsimple's 800x600 pattern without making it visible. Review recheck/native qualification pending.

# Task 7 avatar startup checkpoint

Status: implemented, source-reviewed and locally native-qualified. Entire performance task remains OPEN.

Profile faces and workspace marks use the exact previous DiceBear9.4.3 generator options in a lazy worker. Visible chrome starts with cached SVG artwork or a dimension-preserving local placeholder; cold generation runs after paint during idle time. Stale responses cannot paint another profile's avatar after switching. Fixed privacy guardian is the exact pre-generated SVG and retains its metadata. No external avatar network request is introduced.

One worker coalesces identical requests, caps pending generation at128, times out after10s, and terminates after5s idle. Artwork cache uses one atomic localStorage record bounded to262144 serialized characters and128 entries; in-memory artwork is also bounded. Cache is disposable, versioned and separate from authoritative profile/workspace seeds. Disabled/full/corrupt storage and failed workers preserve controls and placeholders. Source review exposed an orphaned-record bug in the first two-write cache; controlled regression reproduced640KB retained against256KB budget, and atomic persistence corrected it. Review closed.

Saved artwork compatibility: SHA256 fixtures captured before refactoring match four old profile/workspace generator outputs; reviewer independently verified the privacy guardian matches too. Late-result component regression and warm cache/failure/deadline/cap tests pass. Full frontend650, typecheck and ESLint pass. Python launcher13, workspace Rust425, strict Clippy and format/diff checks pass. Logs `/tmp/dive-avatar-full-ui3.log`, `/tmp/dive-avatar-typecheck3.log`, `/tmp/dive-avatar-lint3.log`, `/tmp/dive-avatar-probe-tests2.log`, `/tmp/dive-avatar-permission-rust.log`, `/tmp/dive-avatar-permission-clippy2.log`.

Built App chunk reduced from552292 bytes (169090 gzip) to163390 bytes (46426 gzip), approximately70% smaller minified. Source maps contain zero DiceBear modules in App; generator moved to385204-byte worker (121738 gzip), so this is startup-code removal, not elimination of all shipped bytes. Vite worker bundling follows https://vite.dev/guide/features.html#web-workers . Native CEF worker loading under the existing CSP passed; no CSP relaxation was needed.

Exact native bundle SHA256 `95f8693172336f4c33417cfca9b23c9fb070d39b782799104a47913bc0ee164d`: cold native chrome FCP468ms, worker started495.5ms, both observed avatars fully decoded. Three same-profile restarts decoded cached artwork with zero worker-start marks. All four runs also passed seeded legacy native AR/partitioned storage-access/sensor migration, supported permission-cache reconciliation, native history/IPC, popout close/reattach, natural exit/helper drain. Runs2.97/2.53/2.29/2.21s; incomplete startup returned1. Evidence `target/lifecycle-probes-1788561111800548000`.

Eight startup samples plus warmup on the same bundle all exited normally: cold median698.90ms, warm median710.89ms, controls-ready p95742.83ms. Evidence `target/startup-probes-1788561146168712000`. These are local measurements with the shipping process defaults; historical numbers were taken at a different time and do not establish a controlled causal startup speedup. Every launch uses a disposable profile, mock keychain and volatile AppKit ignore-state option. Installed browser and personal data were untouched.

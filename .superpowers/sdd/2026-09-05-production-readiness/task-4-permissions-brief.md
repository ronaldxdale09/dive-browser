# Task 4 permission isolation slice

Approved plan: docs/superpowers/plans/2026-09-05-production-readiness.md Task4. Implement profile-scoped persistent and temporary permissions, exact requesting-origin validation, original pending request resolution where the pinned runtime actually supports it, safe denial/allow/retry and container separation. Preserve every existing supported permission kind and fail closed on unknown native provenance. Task1 lag/deadline behavior must remain.

Own permissions.rs, relevant permission_set/permissions_list command definitions and registration changes, permission fields in AppState, permission frontend/store/editor code, typed IPC/bindings and focused tests. Coordinate shared files with root. Root owns native probe/lib stress hook/runtime vendor currently; request specific vendor adapter changes only with pinned-API evidence. Native media OS prompts must not be triggered during unattended probes; root will perform real app verification using disposable profiles and mock keychain. No production profile, keychain, installation, or external writes.

Requirements: model scope from authoritative tab->workspace->profile, never trust renderer-supplied profile identity; validate canonical HTTP(S) origin and supported kind; no grant leakage to different profile, frame origin, or navigated/reopened tab. Review legacy unscoped permissions migration conservatively and preserve explainable behavior. Temporary decisions must expire at documented lifecycle and never persist; remembered decisions belong only to their profile. Display scope/duration in permission UI; preserve existing UI default semantics where safe. Clear pending grants on tab close/navigation as required. Check CEF request callback provenance: current adapter may expose only kind, which is insufficient to trust top-level tab URL for iframe-origin requests. Use actual pinned API/CDP source evidence; do not invent origin or browserContextID behavior.

Use behavior red/green regressions (profile separation, canonical origins, malformed kinds/origins, late reply after navigation/close, temporary expiry, persistent reload/restart storage). Tests must call production paths with controllable boundaries. Run focused tests, strict checks, report and commit only owned files. Keep native proof/open requirements explicit. Do not spawn agents. Root is testing frozen Task3 binary while you implement this independent slice.

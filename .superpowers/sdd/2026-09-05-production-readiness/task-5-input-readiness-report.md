# New Tab input readiness

Run6 exact ddcc376d native computer use exposed two losses with immediate shortcut then typeText delivery: cold Settings-to-Palette dropped the text, warm page-to-Palette dropped two leading characters. No focus change was made on that observation alone.

An opt-in bounded chrome trace records performance.now event timing, command source, React Palette layout-effect mount/unmount markers and DOM event target/active tag and allowlisted role. It stores no key contents, input data/value, labels or clipboard data. A separate input-timing.request marker enables it inside the existing mock/profile-gated native sample path. The256-event copied snapshot and dropped count bound retention. Source review clear;38 focused tests then final11 after a TypeScript optional-property correction, final typecheck/ESLint pass. Three Rust UI diagnostic tests and strict Clippy pass. Evidence target/input-timing-tests-final.log, target/input-timing-final-code.log, /tmp/dive-input-timing-rust.log, /tmp/dive-input-timing-clippy.log.

Exact ecb3ac35f1fddd501cae1c8eeaef5912813578a60cc1c245a47bac381eecaa3c run8 rendered Alpha before diagnostics. Its input trace established distinct boundaries:

- Cold native-menu New Tab: command receipt at0ms, seventeen keydown events on BODY at12.4–94.7ms, Palette mounted306.2ms, combobox focused307.7ms; palette remained empty. Input arrived before the lazy Palette existed.
- Warm native-menu New Tab from the page: mount2.2ms, focus3.3ms; sixteen input events arrived in the palette. CUA sent seventeen characters. On Escape, the missing leading character was visible in the previously empty page input. This confirms misrouting across the native page/chrome focus handoff; the chrome trace did not observe that missing event.
- Warm Settings-field New Tab: keyboard command, mount2.3ms, final combobox focus3.2ms, all21 expected input events from16.9ms onward; complete typed string visible.

These are local diagnostic samples, not percentile latency benchmarks. No all-features claim is made. Run8 quit normally with helper drain after261.98seconds (session duration). Evidence target/ui-input-8/native.log and native CUA screenshots in the conversation. Cold loading and native menu focus fixes follow separately; intermittent blank rendering remains unresolved.

## Pinned native keyboard route: source findings, not a runtime diagnosis

The installed [CEF version header](/Users/dvle/Library/Caches/tauri-cef/151.3.24/cef_macos_aarch64/include/cef_version.h:38) identifies CEF 151.3.24, commit `2384915b7b1f0fe5ad1107e48d80c34e86b698d7`, Chromium 151.0.7922.174. The links below pin upstream implementation to that commit.

1. Dive's vendored [keyboard handler](/Users/dvle/.codex/worktrees/dive-production/dive-browser/vendor/tauri-runtime-cef/src/cef_impl/client/keyboard.rs:20) overrides only `on_pre_key_event`. It blocks the specified developer-tools shortcuts when developer tools are disabled; Cmd+T otherwise returns zero without setting `is_keyboard_shortcut`. There is no `on_key_event` override. The pinned Rust binding's [default implementation](/Users/dvle/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/cef-151.8.1+151.3.24/src/bindings/aarch64_apple_darwin.rs:20468) returns zero.
2. CEF's [keyboard contract](/Users/dvle/Library/Caches/tauri-cef/151.3.24/cef_macos_aarch64/include/cef_keyboard_handler.h:45) runs these callbacks on its UI thread. `OnPreKeyEvent` precedes renderer delivery; `OnKeyEvent` follows the renderer/page JavaScript's opportunity to handle the event. [BrowserContentsDelegate](https://github.com/chromiumembedded/cef/blob/2384915b7b1f0fe5ad1107e48d80c34e86b698d7/libcef/browser/browser_contents_delegate.cc#L242) translates the pre-handler return/shortcut flag into the Chromium result and calls the post-handler for eligible unhandled events.
3. [AlloyBrowserHostImpl::HandleKeyboardEvent](https://github.com/chromiumembedded/cef/blob/2384915b7b1f0fe5ad1107e48d80c34e86b698d7/libcef/browser/alloy/alloy_browser_host_impl.cc#L912) skips `skip_if_unhandled` events, then tries the client handler, then the platform delegate. The [native macOS delegate](https://github.com/chromiumembedded/cef/blob/2384915b7b1f0fe5ad1107e48d80c34e86b698d7/libcef/browser/native/browser_platform_delegate_native_mac.mm#L415) calls the main menu's `performKeyEquivalent:` for native key-down events. Thus this explicit CEF menu route is an unhandled-event fallback after page processing. This does not establish that AppKit has no other earlier key-equivalent route, or that run8 used this particular fallback.
4. The pinned [Tauri menu callback](/Users/dvle/.cargo/git/checkouts/tauri-69fbbe4d0942e697/7132030/crates/tauri/src/app.rs:2542) sends `EventLoopMessage::MenuEvent` through the runtime proxy. [RuntimeContext::send_message](/Users/dvle/.codex/worktrees/dive-production/dive-browser/vendor/tauri-runtime-cef/src/runtime.rs:253) handles messages immediately when already inside its main-thread dispatch; otherwise it enqueues and wakes the loop. An unconditional extra queue hop cannot be inferred from `send_event` alone.
5. Once [Dive's menu listener](/Users/dvle/.codex/worktrees/dive-production/dive-browser/apps/desktop/src-tauri/src/menu.rs:246) runs, it focuses the relevant chrome before emitting the frontend command. [Window-aware focus selection](/Users/dvle/.codex/worktrees/dive-production/dive-browser/apps/desktop/src-tauri/src/engine.rs:853) preserves popout targeting. Inside runtime dispatch, [SetFocus handling](/Users/dvle/.codex/worktrees/dive-production/dive-browser/vendor/tauri-runtime-cef/src/webview.rs:690) calls CEF directly. [CEF SetFocus](https://github.com/chromiumembedded/cef/blob/2384915b7b1f0fe5ad1107e48d80c34e86b698d7/libcef/browser/browser_host_base.cc#L443) posts only when off its UI thread; the [macOS implementation](https://github.com/chromiumembedded/cef/blob/2384915b7b1f0fe5ad1107e48d80c34e86b698d7/libcef/browser/native/browser_platform_delegate_native_mac.mm#L380) sets the content view as first responder. There is no source basis here for adding another focus dispatch.

The earliest available CEF interception is `OnPreKeyEvent`, but moving native-menu dispatch there for every modifier chord would change page shortcut precedence. Even an exact Cmd+T-only early interception changes behavior for a page that currently consumes Cmd+T. Preserving that precedence strictly means keeping the renderer opportunity and using the later callback/fallback. A future browser-owned accelerator policy could narrowly intercept only an enabled, exact native launcher equivalent on macOS, retaining nonmatching events, developer-tools policy, keyboard-layout matching and popout targeting; this is a candidate requiring a documented browser-shortcut precedence policy and qualification, not an implemented fix. The approved browser-polish scope permits resolving that routine design choice without another user approval.

The next discriminating diagnostic is gated, passive monotonic receipts at pre-key and post-key callback entry/return, native menu action receipt, runtime immediate-versus-enqueued delivery, app menu listener entry, and chrome focus call completion/first-responder identity. Correlate a fixed launcher classification or native event sequence/timestamp with browser/view IDs and the existing chrome trace; do not record arbitrary key characters, page values or clipboard data. Keep callback returns and shortcut flags unchanged. This separates time before native-menu recognition, time awaiting runtime dispatch, and focus application from the already measured DOM mount/focus interval. Until those receipts exist, the warm leading-character loss is confirmed but its exact native boundary remains open. No production code, build or native launch was performed for this source mapping.

## Eager first-open checkpoint

App now imports Palette with the browser controls, keeping its render conditional on open.palette. Its queries/watchers remain mount-only and other optional panels stay lazy. No global focus behavior changed. The former53.00kB/17.83kB-gzip Palette chunk moves into the App module (final App217.68kB/64.70kB-gzip). Focused38tests passed in /tmp/dive-palette-eager-tests.log and source review cleared lifecycle/popout boundaries.

Idle before/after startup comparison used3 fresh-profile launches and5 same-profile launches per exact binary, plus a warmup. Baseline ecb3ac35 cold/warm median729.38/710.33ms, controls-ready p95745.78ms; final6bad21ac4dd824c318923d5bb1192c8757370451ad15bbd85036c49762cc6ebc cold/warm median716.67/703.64ms, controls-ready p95735.33ms. Every launch exited normally. This small local sample shows no observed regression, not a statistically established startup improvement. Evidence target/palette-startup-before-idle/summary.json and target/palette-startup-after/summary.json. Earlier target/palette-startup-before overlapped compilation and is excluded from the comparison.

Native run9 final6bad21ac rendered Alpha before diagnostics. The first Palette opening from a focused Settings field mounted3.7ms after command receipt and focused5.0ms after receipt; all18 immediately typed characters were visible and traced. The original cold native-menu sample mounted306.2ms after command receipt, but used a different command-entry route, so this is a local cold-readiness comparison rather than a controlled cross-route percentile benchmark.

The separate native focus bug remained: later page-focused Cmd+T sent four leading characters into the page input and16 into Palette. The latter mounted1.3ms and focused2ms after the native-menu command reached JavaScript. No native focus fix is claimed. All renderer/IPC samples answered; no blank window reproduced. Normal Quit/helper drain passed after186.80seconds (session duration). Evidence target/ui-input-9/native.log and CUA screenshots. Canonical pnpm check passed:703 frontend tests with one opt-in benchmark skipped,448 Rust tests,19 Python harness tests, formatting, TypeScript, ESLint, Vite build and strict Clippy (/tmp/dive-palette-eager-final-check.log).


## Native route qualification (run10)

Exact binary `3d473847043b90343143557e6f16e67c99f52b5ed8b44d1d9b9d2e0b61130994` launched with an isolated mock-keychain profile, manual chrome sampling and separately gated native input receipts. Native pixels showed the complete Alpha page and browser controls before any diagnostic sample. All sampled document/IPC requests answered. Normal native Quit and helper drain completed after227.01seconds (session duration). Evidence: `target/ui-native-route-10/native.log` and computer-use screenshots in the conversation.

The native receipt module is inert by default, requires explicit diagnostic/mock/profile gates, retains no typed text and emits at most256 fixed-metadata receipts per process. A Cmd+T raw keydown arms only a500ms window. Six tests passed standalone and module-wrapped; the child-test filter asserts a real test ran in both harnesses. Three app diagnostic tests, strict workspace Clippy and the exact release build passed. Independent review closed both the non-CEF cfg and module-qualified test-filter findings. Synchronous diagnostic output can alter timing; these samples are causal event-order evidence, not uninstrumented performance measurements. Generic runtime events have no message identity, so their proximity alone does not prove command causality.

Two settled page-to-Palette attempts retained complete18/20-character input. The next settled page attempt reproduced the loss: computer use typed19characters, Palette displayed17, and Escape revealed the missing two characters in the previously empty page input. Native receipts establish the boundary:

- Sequence136: Cmd+T entered page browser2 pre-key at1788602587031.242ms Unix.
- Sequences137–138: two following keydowns entered the same page at7031.571/7031.792ms, before Cmd+T post-key at7031.966ms.
- App menu receipt was7036.027ms; chrome browser1 native focus returned7036.156ms.
- Chrome sample5 mounted Palette1.4ms and focused its combobox2ms after JavaScript command receipt; all17 later input events reached it.

This confirms input can already be routed to the old page while the browser-owned New Tab command is waiting for renderer fallback. It does not support adding a later focus hop. A narrow reserved-shortcut path needs to act before renderer delivery and preserve correct window targeting and single dispatch.

A separate rapid Escape→page click→Cmd+T sequence stayed routed to chrome browser1 during closing, toggled the launcher and accepted no input; sample3 records this interrupted transition. It is not counted as a settled page handoff result and remains an additional transition-race acceptance case.


## Early menu candidate: native queue race remains

The macOS-only candidate called the native main menu for exact Cmd+T in pre-key and consumed the event only when accepted. Four policy regressions and source review passed, and the final combined build compiled it. Native run13 exactfdf8a77c confirmed no Cmd+T post-key fallback, but failed immediate typing:15leading characters appeared in the page input and only5in Palette. Receipts show pre-key1788604409760.936ms, proxy9767.647ms,15later page keydowns while queued, app menu9787.709ms and chrome focus9787.887ms. Thus removing renderer fallback alone does not fix the application-queue interval. No fix claim is made; this candidate will be replaced by a per-page direct trusted-chrome handoff with weak ownership and native-window checks. Normal Quit/helper drain passed147.21seconds. Evidence target/ui-native-route-13/native.log and CUA screenshots.


## Native run15: direct New Tab bridge qualification

Exact ad-hoc test bundle SHA256 `71532b430d6d6401d36e302909b1d91e8ad2457086797652927ca745c40eb48e`, PID76865, isolated mock-keychain profile, fixture port61831. Chrome and page visibly rendered before diagnostics. Normal Quit and owned helper drain passed in398.16s (session duration, not latency). Evidence: `target/ui-native-route-15/native.log` and captures.

Computer use preserved full immediate text in five native page-to-chrome routes: initial Alpha, repeat Alpha, rapid Escape/click/reopen, newly-created Beta full URL, and discarded/restored Alpha. Settings DOM CmdT and native File/New Tab also preserved full input. Original page input remained empty. Native receipts show direct submission/focus before subsequent page input in each accepted route; no duplicate native menu path appeared. First route: pre-key1788606112088.595ms, direct-submit2088.627, direct-focus2088.785, next key chrome2089.061. These diagnostic receipt timings include observer overhead and are not a latency benchmark. Restored Alpha changed native Browser identity2→4 and retained routing.

A separate query replacement issue remains OPEN: already-focused Palette CmdA then immediate full URL twice produced malformed text (first `ttp//`, second `ttp://`). Both bursts delivered all27 beforeinput/input events, so missing key delivery is not established. Two plain-text replacements passed, as did a full URL typed immediately after CmdT and address typing after separately confirmed focus. Pinned CEF native Select All fallback is a candidate, not a proved cause. Need selection metadata/edit-command ordering before a production fix. Popout NewTab remains a source-confirmed unsupported command; detach/reattach native routing not yet exercised.

Validation: frontend724tests,19Python,452Rust pass; 4 real-media Rust tests intentionally ignored in normal gate and previously run separately. Typecheck/lint/format/Vite pass. Strict Clippy initially caught doc backticks; corrected and rerun passed. Build passed. Source reviews clear for direct native bridge and frontend fixed event.


## Native run18: Select All overwrite causality

Exactce4e233992003177b1ec79d46357a4b898a7f99bbda5ca016282cb1bbf066a46; PID99268, port64335. Rendered before probe activation; CmdT + before query preserved alltext. First CmdA+fullURL passed; second returned ttp:// instead of http://. Sample4 proves the first character was inserted and then overwritten: sequence117 CmdA begins with caret27/length27;119 beforeinput also caret27;120 input length28/caret28;121 SelectAll changes selection0..28;124 nextbeforeinput sees0..28;125 input length1. Native CmdApre1788608482099.411, post2102.794; first DOM insertion2099.9 precedes SelectAll2103.4. This is a delayed native selection, not lost character delivery. Evidence target/ui-native-route-18/native.log. NormalQuit/helperdrain294.69s.

A narrowly scoped main-chrome text-field fix now synchronously selects focused supported inputs/textarea on exact macOS CmdA and prevents renderer fallback. Existing prevented events, composition/229, other platforms/modifiers, non-text controls, contenteditable and focus handoff remain untouched. Two meaningful RED helper failures then22 focused tests green with actual Palette integration; source review clear. Native post-fix qualification remains pending.


## Native run19: Select All fix qualified

Exact bundle `e21350eee78a5c49d34b54834b419094f8559634810d4b66815aed695b5e2fac`; isolated mock-keychain profile, PID14411, fixture49405. The page and chrome visibly rendered before manual diagnostics. Native New Tab preserved the complete initial query. Five exact CmdA replacements were issued; first three full URLs were observed in AX. The last two immediate AX snapshots were stale while the document reported visibility hidden; the retained DOM ring shows full selection before insertion and final lengths28/27. All five SelectAll native pre receipts reached chrome1; none reached SelectAll post within the256-receipt cap. The first272DOMevents had rolled out, so their selection order is not retained. Evidence `target/screen-ui-19/input-summary.json` and native.log. Raising the occluded window and Escape restored the browser, with original page input empty; this does not resolve the separately observed startup blank window.

The synchronous main-chrome helper is qualified for this reproduction. Page contenteditable/native editing, popout NewTab and broader input/IME coverage remain open. Full frontend756 tests/one optional benchmark skipped, Rust465/four separately exercised real-media tests ignored by normal gate, Python19, formatting/typecheck/ESLint/Vite/strictClippy/build pass. New diagnostic numeric/cast/style issues were corrected before final Rust gate. Native run19 ended by normal Quit with owned helper drain634.65s, a session duration rather than latency measurement.

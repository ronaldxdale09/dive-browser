# Task7 frontend telemetry checkpoint

Status: source-reviewed and frontend-qualified; busy native WebSocket/UI CPU evidence remains OPEN.

Network and console listeners now accumulate bounded display state and publish every33ms. This avoids copying/publishing a complete list per received event. Network accumulation indexes1000 request rows, preserves redirect timing and final response/failure state, keeps the newest200 ordered frames per live request, and ignores frames after row eviction. Console accumulation keeps the newest500 entries with stable IDs. Existing published snapshots and unaffected tabs retain their references; clear/drop removes queued display work so a pending flush cannot resurrect the tab. Synchronous apply/push first flush queued work to maintain ordering. No Rust capture buffer, wire event, MCP or HAR data path changed; backend data retention is unchanged, and this is not an IPC-rate reduction.

Five initial behavioral regressions failed before implementation;16 focused lifecycle/cap/timer/snapshot tests now pass. Full frontend657 pass with1 explicitly opt-in microbenchmark skipped; typecheck, ESLint and Vite build pass. Source review found no actionable issues. Logs `/tmp/dive-ui-telemetry-red.log`, `/tmp/dive-ui-telemetry-timers.log`, `/tmp/dive-ui-telemetry-full2.log`, `/tmp/dive-ui-telemetry-typecheck2.log`, `/tmp/dive-ui-telemetry-lint2.log`, `/tmp/dive-ui-telemetry-build.log`.

Opt-in repeatable microbenchmark uses actual immediate and queued store paths with5000 events, one discarded warmup and5 measured rounds. Every case asserts expected latest entry and publication count. Measured median handling time: console immediate2.513ms vs batched0.439ms; frames immediate1.489ms vs batched0.412ms. Publications5000→1 for each burst. JSON evidence `/tmp/dive-ui-telemetry-benchmark-final.json`; run command is in `apps/desktop/src/store/telemetry.benchmark.test.ts`. These are jsdom store costs with a simple subscriber, not native renderer CPU, energy, IPC transport cost or a30-tab browser benchmark. Entire Task7 remains OPEN.

# Agent provider setup correctness

Native UI inspection showed stale hardcoded dropdown model names alongside different catalog defaults. Setup now renders only available catalog providers, using their actual names. Model identity remains in the existing catalog-driven Default model row. Unknown saved selection falls back to an actual available provider.

Async setup now awaits activation directly, holds controls busy until persistence acknowledgment, and cancels later save/activation/UI continuation after unmount. Removed the delayed500ms fire-and-forget activation. Verification/key-save/prefs failure clears false success feedback and exposes an accessible local error. Clipboard completion is ignored after the setup changes or closes. Native operations already dispatched are not canceled by the UI guard.

Preference writes now offer an opt-in rejectOnError contract for setup, preserving default report-only behavior of existing settings callers. Reviewer reproduced a queue race: failed transactional activation was replayed by a later unrelated snapshot. The corrected branch removes exactly the failed revision and recomputes remaining optimistic state before later snapshots. Serialization/recovery remain intact.

Evidence: three initial Setup tests failed, then passed; actual prefsSet failure reaches the setup alert; unmount-before-verification prevents later save/activate; catalog availability, busy-until-save and queue-recovery checks pass. Reviewer queue regression red then green checks both persisted second snapshot and final provider/model. Focused25 pass (`/tmp/dive-agent-prefs-race-green.log`). Independent re-review clear. Final frontend685 pass/one opt-in benchmark skipped (`/tmp/dive-agent-setup-full-final.log`), TypeScript and ESLint pass; full Rust433 pass (`/tmp/dive-hidden-setup-rust-full.log`). Exact native3fedd4496a95c2c6f1431b81996e8b9599a5782cb34fe724e2e99ba11371c9d7 CUA showed all14 catalog providers with actual names, selected OpenAI with matching gpt-5 default row, and normal Quit/helper drain (`target/setup-ui-native.log`). Interactive67.20s is session duration, not shutdown latency. Four supported permission/history/IPC/popout/reattach/lifecycle runs passed2.02/2.09/1.76/1.92s and negative incomplete-startup exit1 (`target/lifecycle-probes-1788565184243287000`). The same binary separately crashed during opt-in hidden diagnostic creation before service dispatch; that uncommitted diagnostic is under investigation and is not part of ordinary startup or this setup fix.

No claim yet of successful remote provider authentication, stream completion, action approvals or full agent production readiness.

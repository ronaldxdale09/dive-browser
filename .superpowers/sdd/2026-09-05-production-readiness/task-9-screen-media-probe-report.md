# DiveScreen gated media diagnosis

Native run16 on exact71532b (Stage cleanup with old exporter) repeated the run14 error after copied-sidecar recovery/save retry and export settings. The label stayed at Rendering frame1/90, then the generic media error replaced the editor. Try again reopened the same source and exported successfully; a later plain tab roundtrip also exported successfully. Neither resource teardown nor file corruption is established as the cause. Stage's existing error text describes all media errors as decode failure, so that text does not prove MediaError.code3. Progress is emitted every three frames: frame1 means frame1 completed, with the possible early stall at seeks2–4. Encoder back-pressure cannot explain a queue of at most a few frames. Save retry and format edits do not directly reload src; the Stage resource effect depends on playable/stable setter, and the RAF checks exporting before playback/seek changes.

New `screen/mediaProbe.ts` is inert unless the existing manual diagnostic flag `window.__diveUiInputTimingEnabled === true`. It records at most64 entries in `window.__diveScreenMediaProbe`, with exactly fixed enum/numeric fields: atMs,event,phase,generation,exporting,frame,targetMs,code,currentTime,seeking,paused,readyState,networkState. Absent or nonfinite numeric context is null. There are no URLs, source names, media metadata, native error messages, stacks, or arbitrary payloads. Lease setup/current error/release is captured before source reset; Stage passes the lease's captured editor generation. Export phase/frame/target lives in a WeakMap and is updated without per-frame ring entries. Begin, failed/timed-out/cancelled seek and end are recorded. Late error after the lease ceases being current does not record or affect UI. Backend probe owns sanitized readback of this mutable global and native Media-domain errors using its existing chrome CDP session.

Pinned Chromium151 evidence: `third_party/blink/renderer/core/html/media/html_media_element.cc`2149–2204 maps native network/decode states into DOM error codes; `third_party/blink/public/devtools_protocol/domains/Media.pdl`50–62,86–100 exposes native error groups/codes and Media.enable. `media/base/pipeline_status.h`24–76 distinguishes network2/decode3/read9/invalid-state11/disconnected24/OOM26. The next run can distinguish native pipeline/asset-read failure from a frontend seek/lifetime problem. No fix to the unproved native cause is claimed.

Controlled RED:3failed/1passed. GREEN:4 helper tests+15 exporter tests+4Stage lifecycle tests=23passed. Tests prove default inactivity (no store read or ring), finite/fixed schema without source or error text, retention bound/newest64, lease event order and captured generation, exact timeout targetframe2 retained after export cleanup. Scoped ESLint passes. Frontend typecheck initially overlapped parent's selection-field test-first phase; parent completed that correction and reports typecheck green at `/tmp/dive-selection-probe-typecheck.log`. Logs `/tmp/dive-screen-media-probe-{red,green}.log`, copied to `target/screen-media-probe-regression/`. No native launch/build by this agent. Review requested; native diagnosis remains OPEN.

Review identified and corrected diagnostic generation lineage: an old export settling after another editor opens now retains its original generation in the phase context. Orchestration RED captured actual42 instead of expected41; GREEN confirms cancellation/end of41 remain41 after store advances42. Final24 helper/export/Stage tests, scoped ESLint and full frontend typecheck pass. Logs `/tmp/dive-screen-media-generation-{red,green}.log` and corresponding target copies. Re-review pending.

Separate read-only finding sent to parent: ExportDialog.run finally unconditionally clears the current global exporting flag. A pending native finish from closed editorA can therefore resume editorB's Stage while B is exporting. This requires generation/operation ownership on the final state update and a delayed finish regression; it was not changed in the diagnostic slice and is not proven to cause native16.

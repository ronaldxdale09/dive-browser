/** The project's GitHub repository, for changelog and issue links. */
export const REPO_URL = "https://github.com/ronaldxdale09/dive-browser";

/** Where a recorded flow starts when the tab has no URL of its own. */
export const DEFAULT_START_URL = "https://example.com";
